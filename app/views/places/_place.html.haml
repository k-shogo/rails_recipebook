.item
  .image
    .map2{id: "map_canvas#{place.id}"}
  .content
    .name= place.name
    %p.description= "#{place.postcode} #{place.prefecture} #{place.address}"
    .extra
      = place.events.count
      events
